# NBOOG SACCO — Investor Preview
#
# This deployment runs entirely in-memory on a deterministic mock dataset.
# There are no environment variables to configure.
#
# Just `npm install && npm run dev` — or import the repo on Vercel
# and click Deploy.
